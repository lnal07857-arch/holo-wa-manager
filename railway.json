{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "whatsapp-server/Dockerfile",
    "buildContext": "whatsapp-server"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
