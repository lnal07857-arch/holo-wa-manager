services:
  worker-01:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-01
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-1.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-1:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=1
    networks:
      - wa-network

  worker-02:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-02
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-2.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-2:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=2
    networks:
      - wa-network

  worker-03:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-03
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-3.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-3:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=3
    networks:
      - wa-network

  worker-04:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-04
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-4.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-4:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=4
    networks:
      - wa-network

  worker-05:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-05
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-5.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-5:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=5
    networks:
      - wa-network

  worker-06:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-06
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-6.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-6:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=6
    networks:
      - wa-network

  worker-07:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-07
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-7.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-7:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=7
    networks:
      - wa-network

  worker-08:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-08
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-8.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-8:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=8
    networks:
      - wa-network

  worker-09:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-09
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-9.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-9:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=9
    networks:
      - wa-network

  worker-10:
    build:
      context: .
      dockerfile: Dockerfile.wireguard
    container_name: wa-worker-10
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./wireguard-configs/account-10.conf:/etc/wireguard/wg0.conf:ro
      - wa-sessions-10:/app/.wwebjs_auth
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - PORT=3000
      - ACCOUNT_INDEX=10
    networks:
      - wa-network

  nginx:
    image: nginx:alpine
    container_name: wa-nginx
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "3000:80"
    depends_on:
      - worker-01
      - worker-02
      - worker-03
      - worker-04
      - worker-05
      - worker-06
      - worker-07
      - worker-08
      - worker-09
      - worker-10
    networks:
      - wa-network

volumes:
  wa-sessions-1:
  wa-sessions-2:
  wa-sessions-3:
  wa-sessions-4:
  wa-sessions-5:
  wa-sessions-6:
  wa-sessions-7:
  wa-sessions-8:
  wa-sessions-9:
  wa-sessions-10:

networks:
  wa-network:
    driver: bridge
